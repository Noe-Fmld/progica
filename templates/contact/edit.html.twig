{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Modification du contact : {{ contact.firstName }}{% endblock %}

{% block body %}
    <h1>Modifier votre contact</h1>

    {{ include('contact/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_contact_index') }}" class="btn btn-primary mt-2">← Retour à la liste</a>

    {{ include('contact/_delete_form.html.twig') }}
{% endblock %}
