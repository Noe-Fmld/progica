<form method="post" action="{{ path('app_option_delete', {'id': option.id}) }}" onsubmit="return confirm('Etes-vous surs de vouloir supprimer cette option ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ option.id) }}">
    <button class="btn">Supprimer</button>
</form>
